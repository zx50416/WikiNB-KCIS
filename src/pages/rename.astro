---
/**
 * Rename is folded into Add note (edit existing / filename field).
 * Keep this route as a redirect so old bookmarks still work.
 */
const base = import.meta.env.BASE_URL;
return Astro.redirect(`${base}notes`);
---

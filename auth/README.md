# WikiNB KCIS Auth

Google 登入（僅 `@kcis.com.tw`）。設定步驟見 [`../docs/AUTH_GOOGLE_SETUP.md`](../docs/AUTH_GOOGLE_SETUP.md)。

```bash
cp .env.example .env   # 填入 Google 憑證
npm install
npm start
```

預設：http://127.0.0.1:8788
